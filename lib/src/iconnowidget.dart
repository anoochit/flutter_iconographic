import 'package:flutter/material.dart';

class IconoWidget {
  final Widget full;
  final Widget? half;
  final Widget empty;
  IconoWidget({
    required this.full,
    this.half,
    required this.empty,
  });
}
